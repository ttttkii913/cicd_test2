version: "3.8"

services:
  backend:
    image: ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}
    container_name: ${DOCKER_CONTAINER}
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_CONFIG_LOCATION: /config/application-test.yml
    volumes:
      - /home/${EC2_USER}/app/config/application-test.yml:/config/application-test.yml
    restart: always
